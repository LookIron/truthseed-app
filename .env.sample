# ========================================
# Bible API Configuration
# ========================================
#
# The TruthSeed app fetches real Bible verses using docs-bible-api (free, no authentication required).
#
# DEFAULT TRANSLATION: Bible translation to use for fetching verses
# - nvi (Nueva Versión Internacional): Default Spanish translation
# - rvr60 (Reina-Valera 1960): Most widely used Spanish translation
# - Other translations available at https://bible-api.deno.dev
BIBLE_DEFAULT_TRANSLATION=nvi
#
# CACHING & OFFLINE SUPPORT:
# - Verses are cached server-side for 7 days
# - Verses are also cached in IndexedDB for offline access
# - No API key required - completely free
#
# NOTE: The docs-bible-api service is free and requires no authentication.

# App Configuration
NEXT_PUBLIC_APP_NAME=TruthSeed

# Node Environment
NODE_ENV=development

# ========================================
# ADW (Agentic Development Workflow) Configuration
# ========================================

# REQUIRED: Anthropic API Key for Claude Code CLI
# Get your key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Path to Claude Code CLI (defaults to 'claude' if not set)
# Only needed if Claude Code is not in your PATH
CLAUDE_CODE_PATH=claude

# GitHub Personal Access Token (optional)
# Only needed if different from 'gh auth login'
# Get a token from https://github.com/settings/tokens
GITHUB_PAT=your_github_pat_here

# Maintain project working directory (recommended for ADW)
CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR=true

# ========================================
# E2B Sandbox Configuration (Optional)
# ========================================

# E2B API Key for sandbox execution
# Get your key from https://e2b.dev/
# Only needed for sandbox_poc.py and E2B-based workflows
E2B_API_KEY=your_e2b_api_key_here

# ========================================
# Cloudflare Configuration (Optional)
# ========================================

# Cloudflare Tunnel Token for webhook exposure
# Get from Cloudflare Zero Trust dashboard
# Only needed if you want to expose webhook server publicly
CLOUDFLARED_TUNNEL_TOKEN=your_cloudflare_tunnel_token_here

# Cloudflare R2 Configuration for screenshot uploads
# Get credentials from Cloudflare R2 dashboard
# Only needed for ADW review phase with screenshot uploads
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id_here
CLOUDFLARE_R2_ACCESS_KEY_ID=your_r2_access_key_id_here
CLOUDFLARE_R2_SECRET_ACCESS_KEY=your_r2_secret_access_key_here
CLOUDFLARE_R2_BUCKET_NAME=your_r2_bucket_name_here
CLOUDFLARE_R2_PUBLIC_DOMAIN=your_public_domain_here
